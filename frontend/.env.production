# Environment variables for the frontend in production

# Backend API URL - Leave empty to use the same origin for Vercel deployments
# This avoids CORS issues when frontend and backend are deployed together
VITE_API_URL=