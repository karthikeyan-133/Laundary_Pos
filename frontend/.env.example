# For local development, you can set the API URL here
# For Vercel deployments, the API is served from the same domain
# VITE_API_URL=https://your-backend-url.vercel.app

# In production (Vercel), we use relative paths to the same domain
# The actual value will be determined at runtime in the code