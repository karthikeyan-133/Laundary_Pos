# Environment variables for Vercel deployments
# For Vercel, we use the same origin for both frontend and backend
# This avoids CORS issues when frontend and backend are deployed together
VITE_API_URL=