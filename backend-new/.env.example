# cPanel Database Configuration (deprecated - using Supabase now)
# Replace these values with your actual cPanel database credentials

# Database host (using localhost for local development)
# DB_HOST=localhost

# MySQL username (default XAMPP user)
# DB_USER=root

# Database user password (default XAMPP password is empty)
# DB_PASSWORD=

# Database name (we'll create this database)
# DB_NAME=Pos_system

# Database port (default MySQL port)
# DB_PORT=3306

# Use SSL connection (not needed for local development)
# DB_SSL=false

# Supabase Configuration
# ⚠️ SECURITY WARNING: Never commit real credentials to version control
# For Vercel deployment, set these as environment variables in the Vercel dashboard
# SUPABASE_URL and SUPABASE_KEY should be set in Vercel, not here

# For local development, replace with your actual Supabase credentials:
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your-supabase-anon-key-here

# Server port (Vercel will override this)
PORT=3001

# Example environment variables for the backend
# Copy this file to .env and update the values for your local development
# For Vercel deployments, these should be set in the Vercel dashboard

# Database configuration
DB_HOST=localhost
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_NAME=your_database_name
DB_PORT=3306
DB_SSL=false

# For local development, the server will default to port 3003
# PORT should not be hardcoded for Vercel deployments
# PORT will be set by Vercel through process.env.PORT
